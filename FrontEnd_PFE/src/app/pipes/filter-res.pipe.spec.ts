import { FilterResPipe } from './filter-res.pipe';

describe('FilterResPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResPipe();
    expect(pipe).toBeTruthy();
  });
});
